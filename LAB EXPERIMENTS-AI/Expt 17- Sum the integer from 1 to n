% Base case
sum(0, 0).

% Recursive case
sum(N, S) :-
    N > 0,
    M is N - 1,
    sum(M, S1),
    S is S1 + N.

% Main program
:- initialization(main).

main :-
    sum(5, S),
    write('Sum = '),
    write(S),
    nl,
    halt.
